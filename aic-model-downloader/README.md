# aic-model-downloader

Small Rust helper for downloading ai-coustics `.aicmodel` files from the artifact repository.

This functionality is exposed through the `Model::download` function in the `aic-sdk` crate.
